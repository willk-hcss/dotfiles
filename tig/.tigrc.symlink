set vertical-split = no

# Start interactive rebase from tig
# https://agateau.com/2016/starting-an-interactive-rebase-from-tig/
bind main R !git rebase -i %(commit)^
bind diff R !git rebase -i %(commit)^
